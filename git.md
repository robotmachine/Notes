# [git](http://git-scm.com/ "git")

### Undo last commit
`git reset --soft HEAD^`  

### View remote servers
`git remote -v`  
 
### Remove remote servers
`git remote rm foo`  

### Patch mode
`git add -p`  
